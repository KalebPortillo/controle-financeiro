# PostgreSQL config for Controle Financeiro.
#
# Dev: aponta para o Postgres do `infra/docker-compose.yml` (host port 5433).
# Test: mesmo container, banco separado.
# Production: lê DATABASE_URL setado pelo Kamal/Oracle Cloud.

default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: <%= ENV.fetch("DB_HOST", "localhost") %>
  port: <%= ENV.fetch("DB_PORT", 5433) %>
  username: <%= ENV.fetch("DB_USERNAME", "controle_financeiro") %>
  password: <%= ENV.fetch("DB_PASSWORD", "dev") %>

development:
  <<: *default
  database: controle_financeiro_development

test:
  <<: *default
  database: controle_financeiro_test

# Staging local: roda em :3001 contra o mesmo Postgres do dev, banco separado.
# Quando subir staging no Kamal, DATABASE_URL externo cobre.
staging:
  <<: *default
  database: controle_financeiro_staging

production:
  primary: &primary_production
    <<: *default
    database: controle_financeiro_production
    username: controle_financeiro
    password: <%= ENV["DATABASE_PASSWORD"] %>
    host: <%= ENV["DB_HOST"] %>
    port: <%= ENV.fetch("DB_PORT", 5432) %>
  cache:
    <<: *primary_production
    database: controle_financeiro_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: controle_financeiro_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: controle_financeiro_production_cable
    migrations_paths: db/cable_migrate
