# Sentry DSN (project: controle-financeiro-frontend).
# Vite injeta variáveis VITE_* no bundle do browser em build time.
# Sem este valor o SDK fica inerte — app boota mas não reporta erros.
VITE_SENTRY_DSN=
